window.PlaygroundSection = () => {
  return (
    <section id="playground" className="fade-in" style={{ paddingTop: '8rem' }}>
      <h2 className="title" style={{ fontSize: '2.5rem' }}>Simulation Playground</h2>
      <p className="subtitle" style={{ marginBottom: '4rem' }}>See exactly how datasets are learned natively.</p>
      <div className="card" style={{ textAlign: 'center', padding: '4rem', maxWidth: '800px', margin: '0 auto', width: '100%' }}>
        <h3 style={{ marginBottom: '1rem', color: 'var(--text-primary)' }}>Sandbox Mode (Coming Soon)</h3>
        <p style={{ color: 'var(--text-secondary)' }}>Advanced interactive dataset uploads capability pending integration.</p>
      </div>
    </section>
  );
};
